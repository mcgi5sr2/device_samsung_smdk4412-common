<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cyanogenmod.settings.device"
    android:sharedUserId="android.uid.system" >

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Settings">
        <activity
            android:name="com.cyanogenmod.settings.device.DisplaySettings"
            android:launchMode="singleTop"
            android:icon="@drawable/ic_launcher_cmdevicesettings"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:excludeFromRecents="true" >
            <intent-filter>
                <action android:name="com.cyanogenmod.action.LAUNCH_DEVICE_SETTINGS" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="org.namelessrom.device.extra.SettingsActivity"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:targetActivity="com.cyanogenmod.settings.device.DisplaySettings">
            <intent-filter>
                <action android:name="org.namelessrom.action.LAUNCH_EXTRA_DEVICE_SETTINGS" />
            </intent-filter>
        </activity-alias>

        <receiver android:name=".Startup">
            <intent-filter android:priority="100">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
    </application>
</manifest>
